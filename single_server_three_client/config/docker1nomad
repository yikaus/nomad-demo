region = "au"

datacenter = "dc1"

bind_addr = "192.168.0.20"

# Increase log verbosity
log_level = "DEBUG"

# Setup data dir
data_dir = "/tmp/nomad"

# Enable the server
server {
    enabled = true
    bootstrap_expect = 1
}

client {
	enabled = true
	servers = ["192.168.0.20"]
        network_interface = "eth1"
	options = {
          consul.address = "192.168.0.20:8500"
         }
}
